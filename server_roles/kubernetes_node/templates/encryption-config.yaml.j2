# {{ ansible_managed | default('Managed by Ansible — server_roles/kubernetes_node') }}
apiVersion: apiserver.config.k8s.io/v1
kind: EncryptionConfiguration
resources:
  - resources: ["secrets"]
    providers:
      - aescbc:
          keys:
            - name: key1
              secret: {{ k8s_encryption_at_rest_key }}
      - identity: {}
